import { MockedCall } from "./MockedCall";
import { MockedProperty } from "./MockedProperty";
export interface TMocked<T> {
    object: T;
    setup<U>(f: (t: T) => U): MockedCall<U> | MockedProperty<U>;
    verify(errors: Array<any>): any;
    describeMocks(): any;
}
