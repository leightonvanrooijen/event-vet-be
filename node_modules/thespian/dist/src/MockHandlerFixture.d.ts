import { MockedCall } from "./MockedCall";
import { Thespian } from "./Thespian";
import { SuccessfulCall } from "./SuccessfulCall";
export declare class MockHandlerFixture {
    successfulCalls: Array<SuccessfulCall>;
    private handler;
    makeMock(methodName: string, expectedArguments: Array<any>): MockedCall<any>;
    getMock(methodName: string | number | symbol): any;
    successes(): SuccessfulCall[];
    static it(name: string, fn: (fixture: Thespian) => any): void;
}
