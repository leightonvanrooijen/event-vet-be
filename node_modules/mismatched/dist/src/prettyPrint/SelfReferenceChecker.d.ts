export declare class SelfReferenceChecker {
    seen: Array<ContextValue>;
    recurse<T>(context: string, value: any, fn: () => T): T;
    static colouredMessage(context: string): string;
}
interface ContextValue {
    context: string;
    value: any;
}
export {};
