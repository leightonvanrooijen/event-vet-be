import { Tile } from "./Tile";
import { Appender } from "../Appender";
export declare class PseudoCallTile implements Tile {
    private callName;
    private args;
    private withSquareBrackets;
    stringLength: number;
    complexity: number;
    constructor(callName: string, args: Array<Tile> | undefined, withSquareBrackets?: boolean);
    render(appender: Appender): void;
}
