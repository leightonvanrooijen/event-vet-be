"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.printTiles = exports.printSequenceOfTiles = void 0;
function printSequenceOfTiles(tiles, startToken, tokenEnd, stringLength, appender, complexity) {
    appender.add(startToken);
    const indented = stringLength > appender.remaininglineWidth || appender.tooComplex(complexity);
    if (indented) {
        appender.newLine();
        appender.tabRight();
    }
    printTiles(tiles, appender);
    if (indented) {
        appender.newLine();
        appender.tabLeft();
    }
    appender.add(tokenEnd);
}
exports.printSequenceOfTiles = printSequenceOfTiles;
function printTiles(tiles, appender) {
    const length1 = tiles.length;
    for (let i = 0; i < length1; i++) {
        const tile = tiles[i];
        if (i > 0 && tile.stringLength > appender.remaininglineWidth || appender.tooComplex(tile.complexity)) {
            appender.newLine();
        }
        tile.render(appender);
        if (i < length1 - 1) {
            appender.add(", ");
        }
    }
}
exports.printTiles = printTiles;
//# sourceMappingURL=printSequenceOfTiles.js.map