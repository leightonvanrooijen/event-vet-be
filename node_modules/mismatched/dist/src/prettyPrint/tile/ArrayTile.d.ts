import { Appender } from "../Appender";
import { Tile } from "./Tile";
export declare class ArrayTile implements Tile {
    private elements;
    stringLength: number;
    complexity: number;
    constructor(elements: Array<Tile>);
    render(appender: Appender): void;
}
