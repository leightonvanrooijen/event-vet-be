import { Appender } from "../Appender";
import { Tile } from "./Tile";
export declare class ObjectTile implements Tile {
    private elements;
    stringLength: number;
    complexity: number;
    constructor(elements: Array<FieldTile>);
    render(appender: Appender): void;
}
export declare class FieldTile {
    private key;
    private value;
    stringLength: number;
    complexity: number;
    constructor(key: string, value: Tile);
    render(appender: Appender): void;
}
