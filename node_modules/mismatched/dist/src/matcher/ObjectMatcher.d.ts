import { ContextOfValidationError, DiffMatcher } from "./DiffMatcher";
import { MatchResult } from "../MatchResult";
import { Mismatched } from "./Mismatched";
export declare class ObjectMatcher<T extends object> extends DiffMatcher<T> {
    private expectedObject;
    private fieldMatchers;
    private constructor();
    static make<T extends object>(obj: object): any;
    mismatches(context: ContextOfValidationError, mismatched: Array<Mismatched>, actual: T): MatchResult;
    describe(): any;
    static allKeys(actual: object): (string | symbol)[];
}
export declare function concatObjects(objects: Array<object>): object;
