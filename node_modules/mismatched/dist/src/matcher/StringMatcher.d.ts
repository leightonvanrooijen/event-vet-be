import { ContextOfValidationError, DiffMatcher } from "./DiffMatcher";
import { MatchResult } from "../MatchResult";
import { Mismatched } from "./Mismatched";
export declare class StringMatcher extends DiffMatcher<string> {
    private expected;
    private constructor();
    static make(expected: string): any;
    mismatches(context: ContextOfValidationError, mismatched: Array<Mismatched>, actual: any): MatchResult;
    describe(): any;
}
export declare const stringMatcher: {
    match: (expected: string | RegExp) => any;
    startsWith: (expected: string) => any;
    endsWith: (expected: string) => any;
    includes: (expected: string) => any;
    uuid: () => any;
};
