"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fc = require("fast-check");
const assertThat_1 = require("../assertThat");
const match_1 = require("../match");
describe("match.anyOf property tests:", () => {
    it('match.anyOf() always matches if any of the matchers match', () => {
        fc.assert(fc.property(fc.array(fc.constant(2)), (matchers) => (0, assertThat_1.assertThat)(1).is(match_1.match.anyOf([...matchers, 1]))));
    });
    it('match.anyOf() never matches if none of the matchers match', () => {
        fc.assert(fc.property(fc.array(fc.constant(2)), (matchers) => (0, assertThat_1.assertThat)(1).isNot(match_1.match.anyOf(matchers))));
    });
});
//# sourceMappingURL=AnyOfMatcher.propTest.js.map