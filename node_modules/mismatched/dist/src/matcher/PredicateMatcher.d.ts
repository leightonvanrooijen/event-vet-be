import { DiffMatcher, ContextOfValidationError } from "./DiffMatcher";
import { MatchResult } from "../MatchResult";
import { Mismatched } from "./Mismatched";
export declare class PredicateMatcher extends DiffMatcher<any> {
    private expected;
    private description;
    private constructor();
    mismatches(context: ContextOfValidationError, mismatched: Array<Mismatched>, actual: any): MatchResult;
    describe(): any;
    static make<T>(predicate: (v: any) => boolean, description?: any): any;
}
