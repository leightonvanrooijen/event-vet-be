"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fc = require("fast-check");
const assertThat_1 = require("../assertThat");
const match_1 = require("../match");
const AnyMatcher_propTest_1 = require("./AnyMatcher.propTest");
describe("array.contains property tests:", () => {
    it('match.contains() always matches with any element of the array', () => {
        fc.assert(fc.property(fc.array((0, AnyMatcher_propTest_1.anyJavascriptValue)()).filter(a => a.length > 0), value => {
            const index = randomInt(0, value.length - 1);
            (0, assertThat_1.assertThat)(value).is(match_1.match.array.contains(value[index]));
        }));
    });
    it('match.contains() never matches with a non-element', () => {
        fc.assert(fc.property(fc.array((0, AnyMatcher_propTest_1.anyJavascriptValue)()), value => (0, assertThat_1.assertThat)(value).isNot(match_1.match.array.contains(Symbol()))));
    });
});
const randomInt = (min, max) => // min and max inclusive
 Math.floor(Math.random() * (max - min + 1) + min);
//# sourceMappingURL=ArrayContainerMatcher.propTest.js.map