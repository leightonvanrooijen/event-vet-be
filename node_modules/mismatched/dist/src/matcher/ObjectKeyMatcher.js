"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ObjectKeyMatcher = void 0;
const DiffMatcher_1 = require("./DiffMatcher");
const matchMaker_1 = require("../matchMaker/matchMaker");
class ObjectKeyMatcher extends DiffMatcher_1.DiffMatcher {
    constructor(expectedKey) {
        super();
        this.expectedKey = expectedKey;
        this.specificity = expectedKey.specificity + 5; // Encourage this
    }
    static make(expected) {
        return new ObjectKeyMatcher((0, matchMaker_1.matchMaker)(expected));
    }
    describe() {
        return this.expectedKey.describe();
    }
    mismatches(context, mismatched, actual) {
        return this.expectedKey.mismatches(context, mismatched, actual);
    }
}
exports.ObjectKeyMatcher = ObjectKeyMatcher;
//# sourceMappingURL=ObjectKeyMatcher.js.map