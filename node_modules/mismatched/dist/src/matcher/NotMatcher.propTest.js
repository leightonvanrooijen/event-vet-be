"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fc = require("fast-check");
const assertThat_1 = require("../assertThat");
const match_1 = require("../match");
const AnyMatcher_propTest_1 = require("./AnyMatcher.propTest");
describe("match.not() property tests:", () => {
    it("always matches when the argument matcher fails to match", () => {
        fc.assert(fc.property((0, AnyMatcher_propTest_1.anyJavascriptValue)(), (value) => {
            (0, assertThat_1.assertThat)(value).is(match_1.match.not(Symbol()));
        }));
    });
    it("never matches when the argument matcher matches", () => {
        fc.assert(fc.property((0, AnyMatcher_propTest_1.anyJavascriptValue)(), (value) => {
            (0, assertThat_1.assertThat)(value).isNot(match_1.match.not(match_1.match.any()));
        }));
    });
});
//# sourceMappingURL=NotMatcher.propTest.js.map