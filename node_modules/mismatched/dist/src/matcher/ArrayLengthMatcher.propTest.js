"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fc = require("fast-check");
const assertThat_1 = require("../assertThat");
const match_1 = require("../match");
describe("array.length property tests:", () => {
    it('match.every() always matches which each element is matched', () => {
        fc.assert(fc.property(fc.array(fc.integer()), value => (0, assertThat_1.assertThat)(value).is(match_1.match.array.length(value.length))));
    });
    it('match.every() never matches when any element fails to match', () => {
        fc.assert(fc.property(fc.array(fc.integer()), value => (0, assertThat_1.assertThat)(value).isNot(match_1.match.array.length(value.length + 1))));
    });
});
//# sourceMappingURL=ArrayLengthMatcher.propTest.js.map