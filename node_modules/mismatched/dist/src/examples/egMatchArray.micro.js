"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assertThat_1 = require("../assertThat");
const match_1 = require("../match");
describe("array", () => {
    describe("array.match", () => {
        it("literal", () => {
            (0, assertThat_1.assertThat)([1, 2]).is([1, 2]);
            (0, assertThat_1.assertThat)([1, 2]).is(match_1.match.array.match([1, 2])); // long-hand
        });
        it("Embedded matcher", () => {
            (0, assertThat_1.assertThat)([1, 2]).is([1, match_1.match.number.greater(0)]);
            (0, assertThat_1.assertThat)([1, 2]).is(match_1.match.array.match([1, match_1.match.number.greater(0)]));
        });
    });
    it("array.contains", () => {
        (0, assertThat_1.assertThat)([-1, 2]).is(match_1.match.array.contains(match_1.match.number.greater(0)));
    });
    describe("array.every", () => {
        it("literal", () => {
            (0, assertThat_1.assertThat)([1, 1]).is(match_1.match.array.every(1));
        });
        it("matcher", () => {
            (0, assertThat_1.assertThat)([-1, 2]).is(match_1.match.array.every(match_1.match.number.greater(-5)));
        });
    });
    it("array.length", () => {
        (0, assertThat_1.assertThat)([-1, 2]).is(match_1.match.array.length(2));
    });
});
//# sourceMappingURL=egMatchArray.micro.js.map